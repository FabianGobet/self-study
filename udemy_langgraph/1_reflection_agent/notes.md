# 🚀 LangGraph GUI Server

### **📌 Prerequisites**
Ensure that **`requirements.txt`** has been installed, particularly **`langgraph-cli[inmem]`**.

### **🛠️ Start the LangGraph Development Server**
Run the following command:

```sh
langgraph dev --config ./langgraph.json
